{
	"name": "gallery-example",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"check": "pnpm --filter @wiscaksono/sveltekit-route-intercept build && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"lint": "pnpm --filter @wiscaksono/sveltekit-route-intercept build && svelte-kit sync && svelte-check --tsconfig ./tsconfig.json"
	},
	"dependencies": {
		"@wiscaksono/sveltekit-route-intercept": "workspace:*"
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^6.1.0",
		"@sveltejs/kit": "^2.48.4",
		"@sveltejs/vite-plugin-svelte": "^6.2.1",
		"svelte": "^5.39.0",
		"svelte-check": "^4.3.3",
		"typescript": "^5.9.3",
		"vite": "^7.1.12"
	}
}
